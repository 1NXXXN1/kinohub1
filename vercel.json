{
  "buildCommand": "corepack enable pnpm && pnpm install --no-frozen-lockfile && pnpm build",
  "installCommand": "corepack enable pnpm && pnpm install --no-frozen-lockfile",
  "outputDirectory": ".next"
}
